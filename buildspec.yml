version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - public/*
  name: devblog-$(date +%Y-%m-%d)-$(CODEBUILD_START_TIME)
